const fs=require('fs');const path=require('path');const zlib=require('zlib');const root=__dirname;const dir=path.join(root,'.static-parts');const parts=fs.readdirSync(dir).filter(n=>/^part\d+[a-z]?\.txt$/.test(n)).sort((a,b)=>{const A=a.match(/^part(\d+)([a-z]?)\.txt$/),B=b.match(/^part(\d+)([a-z]?)\.txt$/);return Number(A[1])-Number(B[1])||A[2].localeCompare(B[2]);});if(!parts.length)throw new Error('No bundle chunks found');const data=parts.map(n=>fs.readFileSync(path.join(dir,n),'utf8').trim()).join('');const html=zlib.gunzipSync(Buffer.from(data,'base64'));const out=path.join(root,'dist','client');fs.rmSync(path.join(root,'dist'),{recursive:true,force:true});fs.mkdirSync(out,{recursive:true});fs.writeFileSync(path.join(out,'index.html'),html);console.log(`Built ${html.length} bytes from ${parts.length} chunks`);