name: Stage enhanced portfolio payload

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  staged:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Payload staging is complete only after this workflow is replaced."
