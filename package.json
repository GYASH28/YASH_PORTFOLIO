{
  "name": "yash-portfolio-cinematic-system",
  "version": "3.0.1",
  "private": true,
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "npm run build && node scripts/check.mjs"
  }
}
