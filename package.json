{
  "name": "yash-portfolio-cinematic-system",
  "version": "3.0.0",
  "private": true,
  "scripts": {
    "check": "node --check app.js && node --check system.js",
    "build": "node scripts/build.mjs"
  }
}
